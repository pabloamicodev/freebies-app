/* Promo Engine Storefront Runtime — compiled output placeholder.
 * This file is replaced by the actual build output.
 * Build: pnpm --filter @promo/storefront-runtime build
 * Output: packages/storefront-runtime/dist/promo-engine.js → copied here by CI.
 */
(function(){
  if (typeof window === 'undefined') return;
  if (window.__promoEngineLoaded) return;
  window.__promoEngineLoaded = true;
  console.info('[PromoEngine] Runtime placeholder — run pnpm build:storefront to compile.');
})();
